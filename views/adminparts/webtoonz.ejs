outgoing => unanswered, declined, answered
incoming => unanswered, declined, answered

this.setPendingCalls((prev) => {
        const updated = new Map(prev);
        updated.delete(remoteuserid);
        return updated;
    });
    
    
    this.setCalls((prev) => {
        const updated = new Map(prev);
        updated.set(remoteuserid, { ...call });
        return updated;
    });
    
    
    now time to animate the component using framer i want the component to open from the bottom left corner let it grow from the bottom left to expand the entire with of the screen and left it have this fluid motion
    
    putting the entire call states in an object
    and putting the app settings in an object too which includes the theme, ringing tune, 
